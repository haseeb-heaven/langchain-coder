## 2024-05-18 - Streamlit Inputs with Hidden Labels
**Learning:** Inputs with `label_visibility='hidden'` or `'collapsed'` in Streamlit lack sufficient context for screen readers and diminish the UX since the label is missing.
**Action:** Always provide descriptive `help` tooltips and actionable `placeholder` examples for inputs where labels are visually hidden.
