## 2026-07-27 - Add Tooltips to Streamlit Form Submit Buttons
**Learning:** Tooltips on form submission buttons via the `help` parameter provide important context to users, improving both general usability and accessibility, especially in dense UI layouts.
**Action:** Always consider adding a descriptive `help` parameter to interactive elements like `st.form_submit_button()` in Streamlit applications.
